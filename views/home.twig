<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Ansistrano: Just deploy it!</title>

    <!-- Bootstrap -->
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="/components/flat-ui/bootstrap/css/bootstrap.css">
    <link rel="stylesheet" href="/components/flat-ui/css/flat-ui.css">
    <link rel="stylesheet" href="/components/chartist/dist/chartist.min.css">
    <link rel="stylesheet" href="/components/font-awesome/css/font-awesome.min.css">
    <link rel="stylesheet" href="/assets/app.css">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>
<div class="container">
    {% block content %}
        <!--
        <img src="/img/logo.png" style="width: 100px; height: 100px;" alt="Ansistrano" class="brand-logo" />
        -->
        <div class="row">
            <div class="col-xs-12">
                <h1>
                    <a href="https://github.com/ansistrano" target="_blank"><i class="fa fa-github"></i></a>
                    <a href="https://twitter.com/ansistrano" target="_blank"><i class="fa fa-twitter"></i></a>
                    Ansistrano
                </h1>
                <p class="lead">
                    <a href="https://github.com/ansistrano/deploy" target="_blank"><strong>ansistrano.deploy</strong></a> and
                    <a href="https://github.com/ansistrano/rollback" target="_blank"><strong>ansistrano.rollback</strong></a> are Ansible Galaxy
                    roles to easily manage the deployment process for scripting applications such as PHP, Python and Ruby.
                    It's an Ansible port for Capistrano.
                </p>
            </div>
        </div>

        <div class="row">
            <div class="col-xs-12">
                <h4>Wow! {{ deployments.total }} deployments so far!</h4>
                <p>
                    Today, teams using Ansistrano have deployed <strong>{{ deployments.today }}</strong> times already.
                    When considering the whole month, the amount of deployments is <strong>{{ deployments.month }}</strong>
                    and this year we've reached the <strong>{{ deployments.year }}</strong> deployments.
                </p>
            </div>
        </div>

        <div class="row">
            <div class="col-xs-12">
                <h4>
                    Deployments by weekday
                </h4>
                <div class="progress">
                    <div data-toggle="tooltip" title="{{ 1|to_weekday_name }}: {{ deployments.statsByWeekday.percentage.1 }}%" class="progress-bar" style="width: {{ deployments.statsByWeekday.percentage.1 }}%;"></div>
                    <div data-toggle="tooltip" title="{{ 2|to_weekday_name }}: {{ deployments.statsByWeekday.percentage.2 }}%" class="progress-bar progress-bar-warning" style="width: {{ deployments.statsByWeekday.percentage.2 }}%;"></div>
                    <div data-toggle="tooltip" title="{{ 3|to_weekday_name }}: {{ deployments.statsByWeekday.percentage.3 }}%" class="progress-bar progress-bar-danger" style="width: {{ deployments.statsByWeekday.percentage.3 }}%;"></div>
                    <div data-toggle="tooltip" title="{{ 4|to_weekday_name }}: {{ deployments.statsByWeekday.percentage.4 }}%" class="progress-bar progress-bar-success" style="width: {{ deployments.statsByWeekday.percentage.4 }}%;"></div>
                    <div data-toggle="tooltip" title="{{ 5|to_weekday_name }}: {{ deployments.statsByWeekday.percentage.5 }}%" class="progress-bar progress-bar-info" style="width: {{ deployments.statsByWeekday.percentage.5 }}%;"></div>
                    <div data-toggle="tooltip" title="{{ 6|to_weekday_name }}: {{ deployments.statsByWeekday.percentage.6 }}%" class="progress-bar" style="width: {{ deployments.statsByWeekday.percentage.6 }}%;"></div>
                    <div data-toggle="tooltip" title="{{ 0|to_weekday_name }}: {{ deployments.statsByWeekday.percentage.0 }}%" class="progress-bar progress-bar-warning" style="width: {{ deployments.statsByWeekday.percentage.0 }}%;"></div>
                </div>
            </div>
        </div>

        <!--
        {% for weekDayKey, weekDay in deployments.statsByWeekdayAndHour %}
            <div class="row">
                <div class="col-xs-12">
                    <h4>{{ weekDayKey|to_weekday_name }}</h4>
                </div>
            </div>
            {% for hourDayKey, hourDay in weekDay %}
                <div class="row">
                    <div class="col-xs-12">
                        <span style="padding-right: 10px; float: left;">{{ hourDayKey }}:00</span>
                        <div class="progress" style="margin-bottom: 0px; margin-top: 8px; float:none;">
                            <div class="progress-bar" style="width: {{ (hourDay * 100 / deployments.statsByWeekDayAndHourMax)|number_format }}%;"></div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        {% endfor %}
        -->

        <div class="row">
            <div class="col-xs-12">
                <h5>Developed with <i class="fa fa-heart" style="color:Red;"></i> in Barcelona and London</h5>
            </div>
        </div>
    {% endblock %}
</div>

<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="/components/flat-ui/js/jquery-1.10.2.min.js"></script>

<!-- Latest compiled and minified JavaScript -->
<script src="/components/flat-ui/js/bootstrap.min.js"></script>

<script>
    $(function () {
        $('[data-toggle="tooltip"]').tooltip()
    });
</script>
</body>
</html>